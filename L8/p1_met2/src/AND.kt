class AND2 : Gate
{
    override fun output(inputs : List<Int>): Int {
        //To Do
        var result = 1
        for (input in inputs)
        {
            result *= input
        }
        return result
    }
}

class AND3 : Gate
{
    override fun output(inputs : List<Int>): Int {
        //To Do
        var result = 1
        for (input in inputs)
        {
            result *= input
        }
        return result
    }
}
class AND4 : Gate
{
    override fun output(inputs : List<Int>): Int {
        //To Do
        var result = 1
        for (input in inputs)
        {
            result *= input
        }
        return result
    }
}
class AND8 : Gate
{
    override fun output(inputs : List<Int>): Int {
        //To Do
        var result = 1
        for (input in inputs)
        {
            result *= input
        }
        return result
    }
}